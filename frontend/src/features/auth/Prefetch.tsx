// import { useGetTransactionsQuery } from "../transaction/transactionApiSlice";
// import { Outlet } from "react-router-dom";

// const Prefetch = () => {
//   const { data: getTransactions, isSuccess } = useGetTransactionsQuery();

//   if (isSuccess) return <Outlet />;
// };
// export default Prefetch;
